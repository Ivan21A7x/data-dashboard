# Data Dashboard

Backend built with FastAPI to fetch, process, and store weather data.

## Setup

1. Create a `.env` file based on `.env.example`
2. Install dependencies:

pip install -r requirements.txt

3. Run server:

uvicorn app.main:app --reload
